#include <stdio.h>

int main() {
    int a[5] = {10, 50, 30, 70, 20};
    int* ptr = a;
    int max = *ptr;

    for(int i=1;i<5;i++) {
        if (*(ptr+i) > max)
            max = *(ptr+i);
    }

    printf("Largest: %d\n", max);
    return 0;
}

/*
Output:
Largest: 70
*/
